import "jsr:@supabase/functions-js/edge-runtime.d.ts";
const cors={"Access-Control-Allow-Origin":"https://sniperscoop2-spec.github.io","Access-Control-Allow-Headers":"authorization, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json; charset=utf-8","Vary":"Origin"};
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:cors});
async function sha256Hex(value:string){const d=new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value)));return Array.from(d).map(x=>x.toString(16).padStart(2,"0")).join("");}
function getServerKey(){const named=Deno.env.get("SUPABASE_SECRET_KEYS");if(named){try{const p=JSON.parse(named);if(typeof p?.default==="string")return p.default;const first=Object.values(p??{}).find(v=>typeof v==="string");if(typeof first==="string")return first;}catch{}}return Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??null;}
async function rpc(name:string,args:Record<string,unknown>){const url=Deno.env.get("SUPABASE_URL"),key=getServerKey();if(!url||!key)throw new Error("server_credentials_unavailable");const response=await fetch(`${url}/rest/v1/rpc/${name}`,{method:"POST",headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify(args)});const text=await response.text();if(!response.ok)throw new Error(text||`rpc_${response.status}`);return text?JSON.parse(text):null;}
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});if(req.method!=="POST")return json({ok:false,error:"POST_REQUIRED"},405);try{const auth=req.headers.get("authorization")??"";if(!auth.startsWith("Bearer "))return json({ok:false,error:"UNAUTHORIZED"},401);const token=auth.slice(7).trim();if(token.length<40||token.length>256)return json({ok:false,error:"UNAUTHORIZED"},401);const body=await req.json().catch(()=>({}));const sessionHash=await sha256Hex(token);if(body?.action==="status"){const result=await rpc("make_money_get_mining_status_by_session",{p_session_hash:sessionHash});return json({ok:true,mining:result??[]});}if(body?.action==="upgrade"){const level=Number(body?.level),operationKey=typeof body?.operation_key==="string"?body.operation_key:"";if(!Number.isInteger(level)||level<2||level>10)return json({ok:false,error:"INVALID_LEVEL"},400);if(!/^[A-Za-z0-9_-]{16,128}$/.test(operationKey))return json({ok:false,error:"INVALID_OPERATION_KEY"},400);const result=await rpc("make_money_mining_action",{p_session_hash:sessionHash,p_level:level,p_operation_key:operationKey});const row=Array.isArray(result)?result[0]:result;return json({ok:true,level:Number(row?.level??level),name:row?.name??null,cost:Number(row?.cost??0),rate_per_hour:Number(row?.rate_per_hour??0),balance:Number(row?.balance??0)});}const operationKey=typeof body?.operation_key==="string"?body.operation_key:"";if(!/^[A-Za-z0-9_-]{16,128}$/.test(operationKey))return json({ok:false,error:"INVALID_OPERATION_KEY"},400);const result=await rpc("make_money_claim_mining",{p_session_hash:sessionHash,p_operation_key:operationKey});const row=Array.isArray(result)?result[0]:result;return json({ok:true,reward:Number(row?.reward??0),balance:Number(row?.new_balance??0),claimed_at:row?.claimed_at??null,next_claim_at:row?.next_claim_at??null});}catch(error){const m=error instanceof Error?error.message:String(error);if(m.includes("invalid_or_expired_session"))return json({ok:false,error:"SESSION_EXPIRED"},401);if(m.includes("invalid_session"))return json({ok:false,error:"UNAUTHORIZED"},401);if(m.includes("invalid_operation_key"))return json({ok:false,error:"INVALID_OPERATION_KEY"},400);if(m.includes("insufficient_balance"))return json({ok:false,error:"INSUFFICIENT_BALANCE"},409);if(m.includes("invalid_mining_upgrade"))return json({ok:false,error:"INVALID_MINING_UPGRADE"},409);if(m.includes("mining_module_not_found"))return json({ok:false,error:"MINING_MODULE_NOT_FOUND"},404);if(m.includes("operation_key_reused"))return json({ok:false,error:"OPERATION_REUSED"},409);console.error("make-money-mining error",m);return json({ok:false,error:"MINING_SERVER_ERROR"},500);}});
