const ACHIEVEMENTS_API='https://klvpeopoziausjvefaek.supabase.co/functions/v1/make-money-achievements';
let achievementBusy=false;
function achievementKey(){if(window.crypto?.randomUUID)return window.crypto.randomUUID().replace(/-/g,'');const b=new Uint8Array(24);crypto.getRandomValues(b);return Array.from(b,x=>x.toString(16).padStart(2,'0')).join('');}
async function achievementFetch(body){const c=new AbortController(),t=setTimeout(()=>c.abort(),15000);try{return await fetch(ACHIEVEMENTS_API,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${window.mmSessionToken}`},body:JSON.stringify(body),cache:'no-store',signal:c.signal});}finally{clearTimeout(t);}}
function achievementFormat(v){return Number(v||0).toLocaleString('en-US',{maximumFractionDigits:4});}
function renderAchievements(rows){const list=document.getElementById('achievementsList');if(!list)return;list.replaceChildren();for(const a of rows){const row=document.createElement('div');row.className='job-row';const head=document.createElement('div');head.className='job-head';const name=document.createElement('span');name.className='job-name';name.textContent=a.name;const reward=document.createElement('span');reward.className='job-rate';reward.textContent=`+${achievementFormat(a.reward)} MM`;head.append(name,reward);const meta=document.createElement('div');meta.className='job-meta';const desc=document.createElement('span');desc.textContent=a.description;meta.append(desc);const button=document.createElement('button');button.type='button';if(a.claimed){button.textContent='Claimed';button.disabled=true;button.className='secondary';}else if(a.completed){button.textContent=`Claim · ${achievementFormat(a.reward)} MM`;button.disabled=achievementBusy;button.onclick=()=>claimAchievement(a.achievement_id);}else{button.textContent='Not completed';button.disabled=true;button.className='secondary';}row.append(head,meta,button);list.append(row);}}
async function loadAchievements(){if(!window.mmSessionToken)return;const status=document.getElementById('achievementsStatus');try{const r=await achievementFetch({action:'list'}),d=await r.json().catch(()=>null);if(!r.ok||!d?.ok)throw new Error(d?.error||`HTTP ${r.status}`);const rows=Array.isArray(d.achievements)?d.achievements:[];renderAchievements(rows);if(status)status.textContent=`${rows.filter(x=>x.completed&&!x.claimed).length} rewards ready to claim.`;}catch(e){if(status)status.textContent=e.name==='AbortError'?'Achievements request timed out.':'Achievements unavailable.';}}
async function claimAchievement(id){if(achievementBusy)return;achievementBusy=true;try{const r=await achievementFetch({action:'claim',achievement_id:id,operation_key:achievementKey()}),d=await r.json().catch(()=>null);if(!r.ok||!d?.ok)throw new Error(d?.error||`HTTP ${r.status}`);if(typeof window.setBalance==='function')window.setBalance(d.balance);else{window.mmBalance=Number(d.balance||0);const b=document.getElementById('balance');if(b)b.textContent=achievementFormat(d.balance);}await loadAchievements();}catch(e){const s=document.getElementById('achievementsStatus');const m={ACHIEVEMENT_NOT_COMPLETED:'Achievement not completed.',ACHIEVEMENT_ALREADY_CLAIMED:'Already claimed.',OPERATION_REUSED:'Operation already processed.',SESSION_EXPIRED:'Session expired. Reopen the Mini App.'};if(s)s.textContent=m[e.message]||`Achievement claim failed (${e.message}).`;}finally{achievementBusy=false;}}
window.addEventListener('make-money-authenticated',loadAchievements);
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',loadAchievements);else loadAchievements();